version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: life-notes-api
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=development
      - PORT=3002
      - FRONTEND_URL=http://localhost:3000
    networks:
      - life-notes-network

  # PostgreSQL - Will be uncommented in Week 2
  # db:
  #   image: postgres:15-alpine
  #   container_name: life-notes-db
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: lifenotes
  #   ports:
  #     - "5433:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   networks:
  #     - life-notes-network

networks:
  life-notes-network:
    driver: bridge

volumes:
  postgres-data:
